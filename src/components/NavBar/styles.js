import { Colors } from '../../colors';

const styles = {
  navbar: {
    backgroundColor: Colors.black,
  },
  navbarLinks: {
    color: Colors.white,
    fontWeight: '700',
  },
};

export default styles;
